{
  "name": "cloudflare-introduction-handson",
  "private": true,
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "sync-lectures": "npm --prefix site run sync-lectures"
  }
}
