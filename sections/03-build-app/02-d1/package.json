{
  "name": "03-build-app-02-d1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "front": "wrangler pages dev ./public --port 8788",
    "db:create": "wrangler d1 create hitokoto-db",
    "db:setup": "wrangler d1 execute hitokoto-db --local --file=./schema.sql",
    "db:setup:remote": "wrangler d1 execute hitokoto-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
