{
  "name": "02-build-app-04-r2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "front": "wrangler pages dev ./public --port 8788",
    "db:create": "wrangler d1 create hitokoto-db-03-r2",
    "db:migrate:local": "wrangler d1 migrations apply hitokoto-db-03-r2 --local",
    "db:migrate:remote": "wrangler d1 migrations apply hitokoto-db-03-r2 --remote",
    "bucket:create": "wrangler r2 bucket create hitokoto-images-03-r2"
  },
  "dependencies": {
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
